# TODO.md

## Current Work

- Verify that two source files declaring the same package compose into one build when running `go run .`.
- Inspect the result of `go fmt` on the small program.
- Establish package visibility rules through exported and unexported identifiers.
- Introduce basic values, variables, constants, and functions.
- Explore structs, slices, maps, methods, and interfaces before concurrency.
- Return later to goroutines, channels, CSP, and the transputer lineage.

## Maintenance

- Add domain capsules only when stable conceptual generators or useful semantic anchors emerge.
- Replace or remove completed TODO items rather than promoting them automatically into accepted state.
